import Vue from 'vue';
import Vuex from 'vuex';
import design from '@/store/design';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    design
  }
});
